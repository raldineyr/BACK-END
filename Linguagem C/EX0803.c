#include <stdio.h>

int main()
{
    int a, b;

    a = 5;
    b = a + 10;

    if (a < b)

        printf("O valor de %d e menor que o valor de %d", a, b);

    return (0);
}

// ERRANDO DE PROPOSITO.
